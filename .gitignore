node_modules/
.next/
out/
build/
# Build artefacts for the published package (regenerated by npm run build:package)
dist/
*.tgz

# Local databases and scan artefacts — never commit indexed token data.
*.db
*.db-wal
*.db-shm
.dev.db*
.smoke.db*
.e2e/

# Test output
test-results/
playwright-report/
coverage/

# Logs and OS noise
.server.log
*.log
.DS_Store
Thumbs.db

.env
.env.local

# TypeScript incremental build cache
*.tsbuildinfo

# Local agent tooling — not part of this product
.agents/
.claude/
skills-lock.json
